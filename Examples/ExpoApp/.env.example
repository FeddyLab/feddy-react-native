# Copy this file to .env and fill in your Project ID from
# dashboard.feddy.app → Settings → General. The EXPO_PUBLIC_ prefix
# tells Expo to expose the value to the client bundle.
#
# .env is gitignored — only .env.example is committed (with no value).

EXPO_PUBLIC_FEDDY_API_KEY=
